class DeviceModel {
  var id, mac, longitude, latitude, status, type;
  DeviceModel(
      {this.id,
      this.mac,
      this.longitude,
      this.latitude,
      this.status,
      this.type});
}
